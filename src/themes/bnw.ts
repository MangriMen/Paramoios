import { createTheme } from '@mui/material';

const bnwTheme = createTheme({
  typography: {
    fontFamily: 'Eberron',
  },
  palette: {
    primary: {
      main: '#212121',
    },
    secondary: {
      main: '#fafafa',
    },
  },
});

export default bnwTheme;
